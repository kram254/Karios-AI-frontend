---
name: github-push
description: Clone private GitHub repositories and push commits over authenticated HTTPS using a stored Personal Access Token. Use when the user needs to clone a private GitHub repo or commit and push changes directly.
license: Apache-2.0
metadata:
  author: karios-ai
  version: "1.0"
---

# GitHub Push

## Overview
Clone private GitHub repositories and push commits over authenticated HTTPS using a stored Personal Access Token.

## When to use
needs to clone a private GitHub repo or commit and push changes directly

## Quick start
Invoke this skill when you need Clone private GitHub repositories and push commits over authenticated HTTPS using a stored Personal Access Token..
